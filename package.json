{
	"name": "aegisgate-commitlint",
	"version": "1.4.0",
	"private": true,
	"description": "Conventional-commit linting for AegisGate (non-Node Maven project).",
	"scripts": {
		"commitlint": "commitlint --last --verbose"
	},
	"devDependencies": {
		"@commitlint/cli": "21.2.2"
	},
	"engines": {
		"node": ">=22.12.0"
	}
}