{
	"name": "cacherelay-commitlint",
	"version": "1.4.0",
	"private": true,
	"description": "Conventional-commit linting for CacheRelay (non-Node Maven project).",
	"scripts": {
		"commitlint": "commitlint --last --verbose"
	},
	"devDependencies": {
		"@commitlint/cli": "21.2.2"
	},
	"engines": {
		"node": ">=24.21.0"
	}
}